version: "3.9"
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
  # PostgreSQL uzak Supabase projesinde barındırılıyor.
  # Lokal geliştirme için Supabase CLI (`supabase start`) kullanılabilir.
