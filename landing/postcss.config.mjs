/**
 * Bu projede Tailwind veya başka bir CSS ön işlemcisi kullanılmıyor —
 * tüm tasarım src/app/globals.css içinde saf CSS ile yazıldı.
 * Dosya yalnızca ileride bir PostCSS eklentisi eklemek isterseniz duruyor.
 */
const config = { plugins: {} };
export default config;
